<!DOCTYPE html>
<html lang="pt-br">
<head>    
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="description" content="Catálogo de Produtos - AZ Rolamentos. Confira todos os nossos produtos.">
  <title>Catálogo de Produtos | AZ Rolamentos</title>
  
  <!-- Se você não deseja que essa página seja indexada, descomente a meta abaixo: -->
  <!-- <meta name="robots" content="noindex, nofollow"> -->
  
  <!-- Link para o CSS do catálogo -->
  <link rel="stylesheet" href="/css/styles-catalogo.css">
  <link rel="icon" href="/assets/images/icons/logo.jpeg" sizes="32x32" type="image/png">
  <link rel="apple-touch-icon" href="/assets/images/icons/logo.jpeg" type="image/png">
  
  <!-- Font Awesome e fontes, se desejado -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
  <link href="https://fonts.googleapis.com/css2?family=Oswald:wght@400;500;600&display=swap" rel="stylesheet">
</head>
<body>
  <!-- Note que este template pode não ter um header visível, ou o header pode ser diferente, 
       para que a página fique “oculta” no contexto do site -->
  <%- include('partials/header') %>

  <main>
    <section class="catalogo-container">
      <h1>Catálogo de Produtos</h1>
      
      <!-- Exibe todos os produtos -->
      <div class="catalogo-lista">
        <% if (produtos.length > 0) { %>
          <% produtos.forEach(produto => { %>
            <div class="catalogo-item">
              <!-- Se houver imagem associada, ajuste o caminho conforme necessário -->
              <!-- <img src="/assets/images/bearing-images/<%= produto.codigo %>.jpg" alt="<%= produto.descricao %>"> -->
              <h2><%= produto.descricao %></h2>
              <p><strong>Marca:</strong> <%= produto.marca %></p>
              <p><strong>Unidade:</strong> <%= produto.und %></p>
              <p><strong>Saldo:</strong> <%= produto.saldo %></p>
              <p><strong>Valor:</strong> R$ <%= produto.valor %></p>
            </div>
          <% }); %>
        <% } else { %>
          <p>Nenhum produto encontrado no catálogo.</p>
        <% } %>
      </div>
    </section>
  </main>

  <%- include('partials/footer') %>
</body>
</html>
